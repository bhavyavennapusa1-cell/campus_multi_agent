services:
  - type: web
    name: campus-multi-agent
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api_server:app --host 0.0.0.0 --port $PORT
    staticPublishPath: suhani-dashboard-ui
